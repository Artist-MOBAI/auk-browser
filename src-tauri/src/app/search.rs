// use tauri::{AppHandle, Manager};

// #[tauri::command]
// pub fn search(app: AppHandle, url: String) {
//     let window = app.get_webview_window("webview");
//     window.emit("set-url", url).unwrap();
// }
